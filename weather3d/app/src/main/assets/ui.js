// interaction
let drag=false,lx=0,ly=0;canvas.addEventListener('pointerdown',e=>{drag=true;lx=e.clientX;ly=e.clientY;canvas.setPointerCapture(e.pointerId)});canvas.addEventListener('pointermove',e=>{if(!drag)return;let dx=e.clientX-lx,dy=e.clientY-ly;lx=e.clientX;ly=e.clientY;state.focusAvatar=false;state.targetYaw+=dx*.008;state.targetPitch=Math.max(-.24,Math.min(.24,state.targetPitch+dy*.005))});canvas.addEventListener('pointerup',()=>drag=false);canvas.addEventListener('pointercancel',()=>drag=false);
$$('.citybtn').forEach(b=>b.onclick=()=>{state.city=b.dataset.city;$$('.citybtn').forEach(x=>x.classList.toggle('active',x===b));state.stormLab=false;$('#stormBtn').textContent='🌪 Storm Lab';loadWeather();toast(cities[state.city].name)});
$('#refreshBtn').onclick=()=>loadWeather();
$('#avatarBtn').onclick=()=>{state.focusAvatar=!state.focusAvatar;state.targetYaw=state.focusAvatar?.10:-.25;state.targetPitch=.02;toast(state.focusAvatar?'Avatar focus: ON ✨':'Avatar focus: OFF')};
$('#stormBtn').onclick=()=>{state.stormLab=!state.stormLab;$('#stormBtn').textContent=state.stormLab?'🌤 Exit Storm Lab':'🌪 Storm Lab';updateSky();if(state.stormLab){$('#flash').classList.remove('go');void $('#flash').offsetWidth;$('#flash').classList.add('go');toast('Storm Lab is a simulation')}else toast('Back to live weather')};
$('#resetBtn').onclick=()=>{state.focusAvatar=false;state.targetYaw=-.25;state.targetPitch=.02;toast('3D view reset')};
$('#sirenBtn').onclick=()=>playSiren();
function playSiren(){state.siren=true;toast('Siren demo · 4 seconds');try{let A=new (window.AudioContext||window.webkitAudioContext)(),g=A.createGain(),o1=A.createOscillator(),o2=A.createOscillator();g.gain.setValueAtTime(.0001,A.currentTime);g.gain.exponentialRampToValueAtTime(.045,A.currentTime+.1);g.gain.setValueAtTime(.045,A.currentTime+3.5);g.gain.exponentialRampToValueAtTime(.0001,A.currentTime+4);o1.type='sine';o2.type='triangle';o1.frequency.setValueAtTime(510,A.currentTime);o1.frequency.linearRampToValueAtTime(870,A.currentTime+1);o1.frequency.linearRampToValueAtTime(510,A.currentTime+2);o1.frequency.linearRampToValueAtTime(870,A.currentTime+3);o1.frequency.linearRampToValueAtTime(510,A.currentTime+4);o2.frequency.setValueAtTime(255,A.currentTime);o2.frequency.linearRampToValueAtTime(435,A.currentTime+1);o2.frequency.linearRampToValueAtTime(255,A.currentTime+2);o2.connect(g);o1.connect(g);g.connect(A.destination);o1.start();o2.start();o1.stop(A.currentTime+4.05);o2.stop(A.currentTime+4.05);setTimeout(()=>{state.siren=false;A.close()},4200)}catch(e){state.siren=false}}

initGL();loadWeather();
